Transcribe a video and capture screenshots at keyword occurrences, producing a hands-on lab Markdown document.

Run the following command with the provided arguments:

```bash
python3 transcribe.py $ARGUMENTS
```

Stream all output to the user. When complete, confirm the transcript path and the number of screenshots captured.
