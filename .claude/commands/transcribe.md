Transcribe a video file and capture screenshots at keyword occurrences.

Run the following command with the provided arguments:

```bash
python3 transcribe.py $ARGUMENTS
```

Show all output to the user as it runs. When complete, confirm the output location of the transcript and screenshots.
